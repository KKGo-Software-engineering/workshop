

version: '3.2'

services:
  db:
    image: postgres:12.12
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: banking

    ports:
      - "5432:5432"
    volumes:
      - ./db:/docker-entrypoint-initdb.d/